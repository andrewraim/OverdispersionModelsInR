# useDynLib(OverdispersionModelsInR, .registration = TRUE, .fixes = "C_")
useDynLib(OverdispersionModelsInR, find_vertices)

# Export all names
exportPattern(".")

# Import all packages listed as Imports or Depends
import(
  numDeriv,
  CompQuadForm,
  combinat
)
